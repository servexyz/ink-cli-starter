import { h, Vini } from "ink";
// import { withRouter } from "ink-router";
// import PropTypes from "prop-types";

// class Vini extends Component {
//   static propTypes = {
//     history: PropTypes.shape({
//       replace: PropTypes.func
//     })
//   };
//   //this.props.history.replace(newLocation)
// }

// export default Vini;
export default () => "Vini";
